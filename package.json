{
  "name": "project-references-outfile-modules",
  "scripts": {
    "build": "tsc -b src",
    "build:prod": "tsc -b src/tsconfig.prod.json --force",
    "clean": "tsc -b --clean src"
  },
  "devDependencies": {
    "typescript": "^3.5.1"
  }
}
