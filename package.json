{
  "name": "project-references-outfile-modules",
  "scripts": {
    "build": "tsc --build src",
    "build:prod": "tsc --build src/tsconfig.prod.json --force",
    "watch": "npm run build -- --watch",
    "clean": "npm run build -- --clean"
  },
  "devDependencies": {
    "typescript": "^3.5.1"
  }
}
